# Generated by RepoWarden. Each entry below is a Semgrep finding the
# runner classified as a false positive. If one looks like a real bug,
# delete its entry and re-run the CI workflow.
ignores:
  - rule: "javascript.lang.security.audit.hardcoded-hmac-key.hardcoded-hmac-key"
    path: "src/__tests__/loginllama.test.ts"
    line: 128
